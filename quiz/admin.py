# from django.contrib import admin
# from .models import *
# # Register your models here.

# admin.register(Quiz)
# # class QuizAdmin(admin.ModelAdmin):
# #     list_display = ('uid', 'title', 'created_by', 'num_of_questions', 'category', 'time_duration', 'archived', 'created_at', 'updated_at')
# #     search_fields = ('title',)
# #     list_filter = ('created_by', 'category', 'archived', 'created_at', 'updated_at')

# admin.register(Question)
# # class QuestionAdmin(admin.ModelAdmin):
# #     list_display = ('uid', 'question_text', 'quiz', 'img_url', 'num_of_options', 'created_at', 'updated_at')
# #     search_fields = ('question_text',)
# #     list_filter = ('quiz', 'created_at', 'updated_at')

# admin.register(Option)
# # class OptionAdmin(admin.ModelAdmin):
# #     list_display = ('uid', 'option_text', 'question', 'is_correct', 'created_at', 'updated_at')
# #     search_fields = ('option_text',)
#     # list_filter = ('question', 'is_correct', 'created_at', 'updated_at')